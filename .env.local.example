# Local development / tunnels override (example)
#
# Usage:
#   docker compose --env-file .env.local -f docker-compose.yml -f docker-compose.local.yml up --build
#
# Note: Telegram requires HTTPS for WEB_APP_URL and webhook URL. Use a tunnel for local testing.

# Extra diagnostic logging for Mini App issues (0/1).
DEBUG_WEBAPP_LOGS=1

# Public URL for the Mini App (used by bot button). Must be HTTPS in Telegram.
WEB_APP_URL=https://your-miniapp-tunnel.trycloudflare.com/

# Public URL for Telegram webhook (gateway). Must be HTTPS in Telegram.
TELEGRAM_WEBHOOK_URL=https://your-api-tunnel.trycloudflare.com/webhook

# Use catalog-service canonical merged search in bot.
BOT_SEARCH_MODE=catalog
